version: "3.9"

services:
  web:
    command: python manage.py runserver 0.0.0.0:8000 
    env_file:
      - .env   # 👈 use local env file
  db:
    env_file:
      - .env   # 👈 use local env file
